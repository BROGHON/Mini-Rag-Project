services:
  mongodb:
    image: mongo:8.3.11
    container_name: mongodb
    ports:
      - "27007:27017"

    volumes:
      - mongodb_data:/data/db

    restart: always

volumes:
  mongodb_data:

networks:
  backend: